<nav class="navbar navbar-expand-lg p-2">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
            {#<img src="{{ asset('images/logo-quai-antique.png') }}" alt="Logo" width="100" height="30" class="d-inline-block align-text-top">#}
            La Bouche D Gouts
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_homepage') }}">Page d'accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Carte & Menus</a>
                </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Mon compte</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN')  %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin') }}">Admin</a>
                    </li>
                {% endif %}
            </ul>
            <div>
                {% if app.user %}
                    <a type="submit" class="btn btn-sm btn-outline-warning me-3 ms-2" href="{{ path('app_logout') }}">Se deconnecter</a>

                {% else %}
                    <a type="submit" class="btn btn-sm btn-outline-warning me-2" href="{{ path('app_login') }}">Se connecter</a>
                    <a type="submit" class="btn btn-sm btn-outline-warning me-3" href="{{ path('app_register') }}">Créer un compte</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
<div class="container-fluid header p-5">

    <div class="d-flex flex-column justify-content-around py-xl-2 header-title text-center text-xl-start">
        <h1 class="title-element">La Bouche D Gouts</h1>
        <img src="{{ asset('images/46756e3c.png') }}" class="w-25 m-auto m-xl-0 p-sm-1 p-xxl-5" alt="">
        <h2 class="title-element">Notre chef, Râ D Gouts, vous souhaite la bienvenue dans son premier restaurant underground!</h2>
    </div>
    <img src="{{ asset('images/header-img.jpg') }}"  class="header-img" alt="">
</div>
