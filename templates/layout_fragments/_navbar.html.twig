<nav id="my-navbar" class="navbar navbar-expand-lg p-2">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_homepage') }}">
            <img src="{{ asset('images/Logo.png') }}" alt="Logo" width="100" height="100" class="d-inline-block align-text-top">

        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_homepage') }}">Page d'accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_meal_display') }}">Carte & Menus</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_reservation') }}">Réservation</a>
                </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_user_page') }}">Mon compte</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN')  %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin') }}">Admin</a>
                    </li>
                {% endif %}
            </ul>
            <div>
                {% if app.user %}
                    <a type="submit" class="btn btn-sm btn-outline-warning me-3 ms-2" href="{{ path('app_logout') }}">Se deconnecter</a>

                {% else %}
                    <a type="submit" class="btn btn-sm btn-outline-warning me-2" href="{{ path('app_login') }}">Se connecter</a>
                    <a type="submit" class="btn btn-sm btn-outline-warning me-3" href="{{ path('app_register') }}">Créer un compte</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>